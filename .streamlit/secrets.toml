# Streamlit secrets template for deployment or cloud use.
# For local work, .env is normally enough and is preferred.

NEO4J_URI = "bolt://localhost:7687"
NEO4J_USER = "neo4j"
NEO4J_PASSWORD = "your_strong_local_password"
